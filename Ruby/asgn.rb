#!/bin/ruby
def checkResults(user, mastermind)

end
def userInput(*args) 	#Takes an optional argument which will be stored in args. In this case
			#I will use as a prompt
puts args 		#Prints out the Prompt stored in args
gets			#Reads User Input
end			#Returns the result of gets

userInput("What is my name")
